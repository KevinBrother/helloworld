service:
  name: "gateway-service"
  ip: "127.0.0.1"
  port: 8080
  weight: 10
  enable: true
  healthy: true
  ephemeral: true
  group_name: "DEFAULT_GROUP"
  metadata:
    language: "go"
    go_version: "1.23"
    app: "gateway"
    team: "personal"

config:
  data_id: "gateway-config.yaml"
  group: "DEFAULT_GROUP"
  content: |
    service:
      name: gateway-service
      version: "1.0.0"
    routing:
      enabled: true
      timeout: 30