service:
  name: "payment-service"
  ip: "127.0.0.1"
  port: 8083
  weight: 10
  enable: true
  healthy: true
  ephemeral: true
  group_name: "DEFAULT_GROUP"
  metadata:
    language: "go"
    go_version: "1.23"
    app: "payment"
    team: "personal"

config:
  data_id: "payment-config.yaml"
  group: "DEFAULT_GROUP"
  content: |
    service:
      name: payment-service
      version: "1.0.0"
    payment:
      gateway: "mock"
      timeout: 30