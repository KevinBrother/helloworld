services:
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"   # REST / Dashboard
      - "6334:6334"   # gRPC (Go SDK uses this)
    volumes:
      - qdrant_data:/qdrant/storage

volumes:
  qdrant_data:
